import {requireChatGPTUser} from '../chatgpt-auth';
import {db,runtime} from '@/lib/server';
export const dynamic='force-dynamic';
export const metadata={title:'Ansattinnboks',robots:{index:false,follow:false}};
export default async function Inbox(){return <InboxContent/>}
async function InboxContent(){const user=await requireChatGPTUser('/innboks');if(!runtime().ADMIN_EMAIL||user.email.toLowerCase()!==runtime().ADMIN_EMAIL!.toLowerCase())return <section className="section"><h1>Ingen tilgang</h1><p>Innboksen er kun tilgjengelig for nettstedets administrator.</p></section>;try{const result=await db().prepare('SELECT id,name,email,phone,topic,message,design,created_at FROM inquiries ORDER BY created_at DESC LIMIT 100').all<any>();return <section className="section"><span className="eyebrow">BESKYTTET INNBOKS</span><h1 style={{fontSize:'2.5rem',marginBottom:30}}>Henvendelser</h1><p>Viser de siste 100 henvendelsene. Meldinger sendes ikke automatisk videre på e-post.</p>{!result.results.length&&<p style={{padding:'40px 0'}}>Ingen henvendelser ennå.</p>}{result.results.map(r=><article key={r.id} style={{borderBottom:'1px solid #d6e0e4',padding:'30px 0'}}><small>{new Date(r.created_at).toLocaleString('nb-NO',{timeZone:'Europe/Oslo'})} · {r.topic} · {r.id.slice(0,8)}</small><h2 style={{fontSize:'1.5rem',margin:'12px 0'}}>{r.name}</h2><a href={'mailto:'+r.email}>{r.email}</a> {r.phone&&<> · <a href={'tel:'+r.phone}>{r.phone}</a></>}<p style={{whiteSpace:'pre-wrap',marginTop:15}}>{r.message}</p>{r.design&&<p style={{marginTop:15}}>Showroom: {r.design}</p>}</article>)}</section>}catch{return <section className="section"><h1>Innboksen er utilgjengelig</h1><p>Prøv igjen om litt.</p></section>}}
